name: cryptomes
description: An implementation of the Cryptographic Message Syntax
version: 0.1.0
homepage: https://github.com/azulli/cryptomes
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  asn1dart:
    git:
      url: https://github.com/azulli/asn1dart.git
      ref: v0.3.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
